# Shared env — copy to server/.env (and optionally root .env)
MONGODB_URI=mongodb+srv://USER:PASS@CLUSTER.mongodb.net/pixelwall?retryWrites=true&w=majority
PORT=3001
CLIENT_ORIGIN=http://localhost:5173
GRID_SIZE=50
COOLDOWN_MS=10000
